{
  "name": "murmuration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GPU-simulated starling murmuration",
  "scripts": {
    "dev": "vite --port 5273",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 5273",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
